spring:
  datasource:
    url: jdbc:postgresql://localhost:5440/posts
    username: admin
    password: admin
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: validate
    database: postgresql
    database-platform: org.hibernate.dialect.PostgreSQLDialect
  flyway:
    create-schemas: true
server:
  port: 9020